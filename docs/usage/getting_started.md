# Getting Started

To get started with `theme_extensions_builder`, follow these steps:
1. Define your theme classes with the appropriate annotations (`@ThemeExtensions` or `@ThemeGen`).
2. Run the build runner to generate the necessary code.
3. Add the generated theme extensions to your `ThemeData`.
4. Access your theme values directly from the `BuildContext` in your widgets.